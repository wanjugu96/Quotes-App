import { HighestvotesDirective } from './highestvotes.directive';

describe('HighestvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestvotesDirective();
    expect(directive).toBeTruthy();
  });
});
